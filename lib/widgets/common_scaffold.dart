import 'package:flutter/material.dart';

class CommonScaffold extends Scaffold {
  const CommonScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
